- [x] `GET /api/v1/metrics-json/json` 返回 JSON 格式指标数据（非 Prometheus 文本格式）
- [x] 响应包含 `total_requests`、`requests_per_second`、`latency_ms_p50`、`latency_ms_p95`、`error_rate` 字段
- [x] 响应包含 `status_codes` 各状态码分布
- [x] 响应包含 `endpoint_latency` 各端点平均延迟列表
- [x] 响应包含 `cpu_percent`、`memory_percent` 系统资源使用率
- [x] 路由已注册到 `router.py` 中
- [x] 前端 `metricsApi.getMetrics()` 方法可用并已导出
- [x] 仪表盘顶部展示 5 个性能指标卡片（QPS、P95 延迟、错误率、CPU、内存）
- [x] 性能指标卡片每 10 秒自动刷新
- [x] 仪表盘新增请求量 & 延迟趋势图（双 Y 轴 ECharts 折线图）
- [x] 健康度状态根据实际错误率动态变化（绿/橙/红）
- [x] 报告产出统计图和最新报告列表保持不变
- [x] `psutil` 依赖已添加到 `requirements.txt`
- [x] 无 lint/TypeScript 错误
- [x] 离开仪表盘页面时自动清除定时器