# Checklist

- [x] AiModelConfig 数据库模型正确创建，含 id/provider/model_name/model_type/base_url/api_key(is_encrypted)/is_default/created_at/updated_at 字段
- [x] 首次启动迁移：ai_model_configs 表为空时，从旧 runtime_config.json 创建 LLM 默认记录
- [x] GET /api/v1/settings/ai-models 支持按 model_type 筛选，返回正确列表
- [x] POST /api/v1/settings/ai-models 创建新配置，同类型首个自动设默认
- [x] PUT /api/v1/settings/ai-models/{id} 更新配置，字段校验正确
- [x] DELETE /api/v1/settings/ai-models/{id} 删除配置，删除默认时自动转移
- [x] POST /api/v1/settings/ai-models/{id}/set-default 正确切换默认（同类型仅一个默认）
- [x] GET /api/v1/settings/ai-models/default?model_type= 返回对应默认配置
- [x] POST /api/v1/settings/ai-models/{id}/test LLM 模型测试连接正常（成功/失败均正确返回）
- [x] POST /api/v1/settings/ai-models/{id}/test embedding 模型测试连接正常
- [x] POST /api/v1/settings/ai-models/{id}/test rerank 模型测试连接正常
- [x] runtime_config.get_default_model_config() 能从数据库读取各类型默认配置
- [x] EmbeddingService 优先使用 embedding 默认配置，无默认时 fallback 到 settings
- [x] AnalyzerAgent 优先使用 llm 默认配置，无默认时 fallback 到 settings
- [x] ReporterAgent 优先使用 llm 默认配置，无默认时 fallback 到 settings
- [x] 前端 AI 模型配置页按 LLM/embedding/rerank 三区域正确展示
- [x] 前端新增/编辑/删除配置功能正常
- [x] 前端设为默认功能正常（切换默认标记）
- [x] 前端测试连接功能正常（含加载状态和结果提示）
- [x] 自动化调度与分发通道独立页面/区域正常展示
- [x] 系统设置页正确分离 AI 配置与自动化调度，不再混在同一页
- [x] 旧有 GET/PUT /api/v1/settings（temperature/max_tokens）继续可用